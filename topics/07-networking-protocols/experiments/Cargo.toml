[package]
name = "networking-experiments"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
bytes = "1"

[[bin]]
name = "server"
path = "src/bin/server.rs"
